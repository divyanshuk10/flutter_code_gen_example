name: flutter_code_gen
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  custom_widget_annotation:
    path: packages/custom_widget_annotation/

  build: ^2.1.1
  source_gen: ^1.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  custom_widget_generator:
    path: packages/custom_widget_generator/

flutter:
  uses-material-design: true